# NEXUS-U Reproduction Bundle

The blind corpus and scoring labels are deliberately stored in separate directories. Run inference against `blind/corpus.json`, seal prediction hashes, then open `scoring/labels.json`.

This bundle enables third-party replay but does not itself prove organizational independence.
